{
  "name": "continentalusa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:edge": "wrangler build --cwd edge-worker",
    "deploy:edge": "wrangler publish --cwd edge-worker"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "openapi-typescript-codegen": "^0.29.0",
    "typescript": "^4.9.5"
  }
}
